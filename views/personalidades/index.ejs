<%- include("../partials/header") %>

<div class= "container">
        <header class="jumbotron"> 
                <div class="container">
                    <h1>Bem Vindos à Rinchoa</h1>
                    <p>Veja as personalidades desta linda terra</p>

                       <p>
                          <a class="btn btn-primary btn-lg addButton" href="/personalidades/new">Adicionar nova Personalidade</a>
                        </p>
                </div>
        </header>
                
        <div class ="row text-center" style="display:flex; flex-wrap: wrap;">
                <% personalidades.forEach(function(personalidade){ %>
                        <div class="col-md-3 col-sm-6">  
                           <div class="thumbnail">
                             <img src="<%= personalidade.image %>">
                             <div class="caption">
                                     <h4><%= personalidade.name%></h4>
                             </div>
                             <p>
                                     <a href="/personalidades/<%= personalidade._id %>" class="btn btn-primary">Mais informação</a>
                             </p>
                           </div>      
                        </div>
                <% }) %>
        </div>
</div>

<%- include("../partials/footer") %>
